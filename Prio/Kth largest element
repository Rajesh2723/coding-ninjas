#include<bits/stdc++.h>
int kthLargest(int* arr, int n, int k) {
    vector<int>v;
    sort(arr,arr+n,greater<int>());
    for(int i=0;i<n;i++){
        v.push_back(arr[i]);
    }
    return v[k-1];
}
